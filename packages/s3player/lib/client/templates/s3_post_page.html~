<template name="s3PostPage">

  <div class="post">
      <p><span class="title" style="font-size: 32px;line-height: 1.2;">{{title}} </span></p><br>
         {{#if imageLink}}
        <div class="imagestuff">
     <a href="{{postLink}}" target="{{postTarget}}"><img src="{{imageLink}}" width="580"></a>
        </div>
      {{/if}}

  {{#if videoLink}}
      <div class="videostuff">
         <a href="{{postLink}}" target="{{postTarget}}"><video id="ourVideo" controls>
            <source src="{{videoLink}}" >
          </video>
         
        </a>
        </div>
     {{/if}}

     <div class="bodystuff">
      {{#if body}}
      {{> UI.dynamic template=post_body}}
    {{/if}}

    <div class="theguy">
    <p><b>Written by</b> <span class="author"> {{authorName}}</span><img class="{{cssClass}}" src="{{url}}"/></p>
    </div>
    </div>

     <div class="extrastuff">

     <div class="votes">
      <span class="points">{{baseScore}} <span class="unit">{{pointsUnitDisplayText}} </span></span>
      {{> UI.dynamic template=postVote}}
      </div>

     <div class="time">
      <p><span class="post-time">Posted {{ago}}</span></p>
      </div>

      <div class="share">
      {{> UI.dynamic template=postShare}}
      </div>

      </div>

    {{> UI.dynamic template=comment_form}}
    {{> UI.dynamic template=comment_list}}
  </div>

   {{#if categories}}
 <div class="sideshit">
 <h3>Related Posts</h3>
  {{#each postca}}
   {{> UI.dynamic template=postpagesidebar}}
  {{/each}}
</div>
{{/if}}

</template>
